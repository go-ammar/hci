<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hciscreens" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HCIScreens.NoActionBar" >
        <activity android:name=".ui.AboutDetails" />
        <activity android:name=".loginActivity" />
        <activity
            android:name=".sopViolations"
            android:theme="@style/Theme.MaterialComponents.Light" />
        <activity
            android:name=".mapfile"
            android:label="@string/title_activity_mapfile"
            android:theme="@style/Theme.HCIScreens.NoActionBar" />
        <activity android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>